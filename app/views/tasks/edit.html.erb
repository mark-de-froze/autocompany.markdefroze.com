<% content_for :links do %>
  <%= link_to 'Back', order_tasks_path(@task.order), class: 'btn btn-light' %>
  <%= link_to 'Show', [@task.order, @task], class: 'btn btn-light' %>
  <%= link_to 'Destroy', [@task.order, @task], method: :delete, class: 'btn btn-danger', data: { confirm: t('confirm') } %>
<% end %>

<%= render 'form', task: @task %>

