<% content_for :links do %>
  <%= link_to 'Back',  polymorphic_path([@trackable, History]), class: 'btn btn-light' %>
  <%= link_to 'Show', [@trackable, @history], class: 'btn btn-light' %>
  <%= link_to 'Destroy', [@trackable, @history], method: :delete, class: 'btn btn-danger', data: { confirm: t('confirm') } %>
<% end %>

<%= render 'form', history: @history %>

