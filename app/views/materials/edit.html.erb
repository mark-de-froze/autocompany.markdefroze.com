<% content_for :links do %>
    <%= link_to 'Back', order_materials_path(@material.order), class: 'btn btn-light' %>
    <%= link_to 'Show', [@material.order, @material], class: 'btn btn-light' %>
    <%= link_to 'Destroy', [@material.order, @material], method: :delete, class: 'btn btn-danger', data: { confirm: t('confirm') } %>
<% end %>

<%= render 'form', material: @material %>

